mcp>=1.0.0,<2.0.0
google-cloud-storage>=2.10.0
pydantic>=2.0.0
gitpython>=3.1.0
pyyaml>=6.0
jsonschema>=4.0.0
google-cloud-securesourcemanager>=0.6.0
# >=0.1.60: workers need the tools=[] option (0.1.12) and working
# setting_sources=[] isolation (fixed in 0.1.60; silently dropped before).
claude-agent-sdk>=0.1.60
google-antigravity>=0.1.0
# Live discovery (STATE_DISCOVERY_LIVE) only. Imported lazily by
# discovery_livescan_0/eks_auth.py so the rest of the server runs without
# them; the tool reports an actionable install line when they are absent.
boto3>=1.34.0
kubernetes>=29.0.0
# The live-discovery e2e test (discovery_livescan_0/e2e_test.py) mints its
# fake control plane's certificate with it; a transitive dependency of
# google-auth already, declared so a venv built from this file runs the test.
cryptography>=42.0.0
